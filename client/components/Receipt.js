import React from "react";

const Receipt = () => {
  //banners about order being places
  //Order number
  //date
  //records of billing address, shipping address, shipment info, payment info (last 4 ddigits and name on card)
  //record of purchase: item, price, total (broken down into subtotal, shipping, sales tax, order total)
  //keep shopping button
};
